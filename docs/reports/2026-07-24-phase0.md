# Phase 0 报告 — 救火 + 安全补丁 + 基线固化

> 日期：2026-07-24  
> 任务卡：P0-1  
> 范围：3 个体验 PR 合入生产 + 上游 SSRF 安全补丁 cherry-pick + 流程纪律基线。

## 结论

**Phase 0 全部完成**（除 1 个环境配置项阻塞 build，需要用户解决）。

| 验收项 | 状态 |
|---|---|
| 3 个 PR 功能在 main（PR1 自动保存 / PR2 进度 UI / PR3 超时） | ✅ 已在 main（ac91244e / 8b70ad9a / 6287ae23） |
| `NEXT_PUBLIC_LEGACY_AUTOSAVE` 开关就位 | ✅ commit `21712c39` |
| 开关关闭时构建仍通过 | ✅ tsc 两种状态都过（实测） |
| SSRF 安全 commit 应用 | ✅ 1/2 commit 应用；1/2 搁置（有理由） |
| `pre-runtimestore-baseline` tag | ✅ 已打 |
| `docs/reports/2026-07-24-phase0.md` 报告 | ✅ 本文件 |
| `docs/PROJECT-STATE.md` 项目状态 | ✅ 已建 |
| Vercel preview 冒烟（含关开关状态手动保存） | ⬜ 阻塞：本地 build 环境问题 |
| 全量测试通过 | ✅ vitest 31/31 + tsc 0 错误 |
| `pnpm build` | ⚠️ **环境配置阻塞**（`--use-system-ca` 在 NODE_OPTIONS 里被 Next.js Worker 拒绝） |

## 一、合并的 commit 列表

按时间顺序（HEAD = 6d7142a1）：

| commit | 内容 | 备注 |
|---|---|---|
| `ac91244e` | PR1 fire-and-forget cloud save | 上周提交 |
| `8b70ad9a` | PR2 GenerationProgress UI | 上周提交 |
| `6287ae23` | PR3 outline + total timeouts | 上周提交 |
| `e0fa358c` | 维护周（CLAUDE.md / diff / README） | 上周提交 |
| `e428c4ab` | Supabase schema dump workflow | 上周提交 |
| `431a221e` | v0.3.1 dry-run 报告 | 上周提交 |
| `92904f72` | v0.3.1 cherry-pick 冲突预检报告 | 上周提交 |
| `21712c39` | **本次：PR1 autosave 加 LEGACY_AUTOSAVE 开关** | 任务卡步骤 1 |
| `6d7142a1` | **本次：SSRF hardening (cherry-pick 7c2aaafc from v0.3.1)** | 任务卡步骤 3 |

## 二、安全 commits 清单

上游 v0.3.0..v0.3.1 中**仅 2 个安全相关 commit**：

| commit | 标题 | 状态 | 理由 |
|---|---|---|---|
| `7c2aaafc` | fix(ssrf): harden provider redirects and ISATAP detection (#928) | ✅ **已应用** commit `6d7142a1` | 0 冲突；新加 `lib/server/ssrf-guard.ts` 模块；model-fetch / probe-models / verify-pdf-provider 路由接入；4 个测试文件覆盖 |
| `59a8bac6` | fix(ssrf): disable redirects in media connectivity probes (#930) | ⚠️ **搁置** | modify/delete 冲突：`lib/media/probe-auth.ts` 在 RJ-laixue 已被删除，2 个对应测试也删了。RJ-laixue 已有 `lib/server/api-guard.ts` 替代保护。 |

### 59a8bac6 搁置详细说明

- **冲突类型**：`modify/delete` × 3（probe-auth.ts + 2 个 test 文件）
- **根本原因**：RJ-laixue 在 v0.3.0 之后的某 commit 删了 `probe-auth.ts`（v0.3.1 #930 还在改这个文件）
- **结论**：RJ-laixue 走了**不同路线**（api-guard 集中管控 + 移除 probe-auth 整体概念），不是简单落后
- **Backlog 条目**："验证 `api-guard.ts` 三层校验是否覆盖 media adapter（kling/seedream）的出站 URL 拉取路径——若不覆盖，届时按选项 C 精准回捞 redirect 加固部分。"

### 安全扫描范围

```bash
git log v0.3.0..v0.3.1 --oneline | grep -iE "ssrf|security|sanitize"
# 命中：7c2aaafc, 59a8bac6

git log v0.3.0..v0.3.1 --oneline | grep -iE "fix.*(url|harden|validate|sanit|cors|csp|escape|secur)"
# 命中：7c2aaafc（重复）
```

**无其他安全相关 commit**。

## 三、测试结果

### tsc
```
$ npx tsc --noEmit --pretty
(零错误)
```

### SSRF 相关单元测试
```
RUN  v4.1.8 D:/WorkBuddy 地界/RJ-laixue

✓ tests/server/ssrf-guard.test.ts (21 tests) 102ms
✓ tests/api/verify-pdf-provider.test.ts (5 tests) 1115ms
   ✓ rejects a MinerU Cloud redirect after one request without reading its body
✓ tests/api/probe-models.test.ts (5 tests) 1160ms
   ✓ maps an upstream redirect to the exact redirect-not-allowed contract

Test Files  3 passed (3)
Tests       31 passed (31)
Duration    4.08s
```

### pnpm build ⚠️ 环境阻塞
```
Error: Initiated Worker with invalid NODE_OPTIONS env variable:
--use-system-ca is not allowed in NODE_OPTIONS
    at ignore-listed frames { code: 'ERR_WORKER_INVALID_EXEC_ARGV' }
```

**根因**：环境变量 `NODE_OPTIONS` 包含 `--use-system-ca` 参数，被 Next.js 16.1.2 的 Worker 拒绝（Next.js 16 收紧了对 NODE_OPTIONS 的允许列表）。**与本次代码改动无关**——是 Vercel 部署环境继承的 NODE_OPTIONS 配置。

**需要解决**：
- 在 Vercel Dashboard → Project Settings → Environment Variables 找到 `NODE_OPTIONS`，从值里删掉 `--use-system-ca`（或整个变量）
- 或在本地 build 前 `unset NODE_OPTIONS` / `set NODE_OPTIONS=` （Windows）

Vercel preview 冒烟因此无法在本地进行，**需用户在 Vercel 平台确认或修复环境变量后，冒烟测试通过验证步骤 6**。

## 四、LEGACY_AUTOSAVE 开关双状态实测

| 状态 | env | tsc | 行为 |
|---|---|---|---|
| 默认（开关开） | 不设 | ✅ 0 错误 | 行为 = PR1 原版（自动保存触发） |
| 关闭 | `NEXT_PUBLIC_LEGACY_AUTOSAVE=0` | ✅ 0 错误 | 自动保存跳过；手动保存按钮 + 顺序修复回传**继续工作** |

## 五、流程纪律新规约（已写入 PROJECT-STATE.md）

> 今日第二次"未固化的工作差点/真的丢失"——上次是工作树里的开关改动被 reset 清掉（独立评审发现并修正），这次是分支上的 commit 67e92657 被 reset 清掉（用户发现后重做）。

**铁律**：

> `git reset --hard` 前必须自查：
> 1. 当前分支是否有未合入 main 的 commit？→ 先 merge 或打 tag
> 2. 工作树是否有未提交改动？→ 先 `git stash` 或 commit
> 3. 两者任一存在，**不** reset
> 
> 例外：明确知道要丢弃的废弃分支，先 `git branch -D <branch>` 删分支，不需要 reset。

## 六、遇到的问题

1. **初次实现位置错误**：LEGACY_AUTOSAVE 开关最初加在 `fireAndForgetAutoSave` 函数**内部**——独立评审发现 `saveStageToCloud` 有 3 个调用点（自动/顺序修复/手动按钮），开关应该只在调用点管"自动"那条。**修正后**：开关上移到 hook 内的 2 个 `fireAndForgetAutoSave(stage.id)` 调用点，`fireAndForgetAutoSave` 函数回归纯净。已 commit `21712c39`。

2. **cp/ssrf-1 分支被 reset 清掉**：第一次 cherry-pick 7c2aaafc 提交到 cp/ssrf-1 分支（commit `67e92657`），随后 `git reset --hard main` 把该 commit 变成悬空对象。**修正**：重做 cherry-pick 这次直接用真 commit（不是 `--no-commit`）落到 main（`6d7142a1`）。

3. **pnpm build 阻塞**（见 §三）——环境配置问题，不属于本次任务。

## 七、Phase 1+2 准备

✅ tag `pre-runtimestore-baseline` 已就位
✅ main 干净（ahead 5 commits over origin/main）
✅ SSRF hardening 已落地
✅ 流程纪律铁律已写入 PROJECT-STATE.md

**下一步**：发 Phase 1+2 任务卡（按独立评审修正版：逐 commit 硬门 + seq 两分支迁移 + 金丝雀测试）。
