# 学习管理与 AI 教学闭环：进度与交接

日期：2026-08-13  
当前主线：Gate 2E（教学驾驶舱与数据中心）

## 已完成

1. 任务闭环：任务创建、发布、名单、分享链接、发布时课程快照、学员任务列表、PC/移动端任务学习入口。
2. 进度闭环：任务学习事件、有效时长、完成度与掌握度分离、任务与课程包进度。
3. 管理闭环：任务总体、课程组合进度、学员明细、AI 简报与经教师确认的补学草稿。
4. 身份模型：教师/管理员同时拥有学习身份，可在同一学员名单中被分配任务；教师保有其创建和管理权限。
5. 多课程任务：一个任务可组合多门课程；课程可被多个任务复用；任务与课程数据口径分离。
6. 驾驶舱：课程管理、学习任务、AI 创课、任务数据看板及 AI 数据问答已接入。
7. 数据中心双视图：
   - 任务运营：任务总体、课程组合、学员关注项；
   - 课程资产：课程跨任务使用、学习人数、完成率、时长；进入课程页后查看章节分析。

## 已执行的生产 SQL

- `supabase-learning-tasks-v1.sql`
- `supabase-learning-tasks-gate1c.sql`
- `supabase-learning-tasks-gate1e.sql`
- `supabase-learning-tasks-gate1f.sql`
- `supabase-learning-tasks-gate2a.sql`
- `supabase-learning-tasks-gate2b.sql`
- `supabase-learning-tasks-gate2g-staff-learners.sql`

## 当前待处理的紧急项

`publish_task_course_package` 在生产环境的 token 生成漏用了 `extensions.` schema 前缀。热修复文件已生成但尚未确认执行：

- `supabase-learning-tasks-gate2b-publish-hotfix.sql`

执行后，可再次发布原草稿任务；它不会重建任务或名单。

## 当前未提交的工作区内容

本次 2E 第二项将涉及：

- `app/api/admin/course-analytics/route.ts`
- `components/course-analytics-board.tsx`
- `components/teaching-data-board.tsx`
- `tests/api/course-analytics.test.ts`
- 本交接报告

发布热修复相关文件应独立提交，勿与 2E 混合：

- `supabase-learning-tasks-gate2b.sql`
- `supabase-learning-tasks-gate2b-publish-hotfix.sql`
- `tests/api/learning-task-course-package-publish-hotfix.test.ts`

此外工作区存在多份 `.tmp-*`、`docs/user-guide/`、`scripts/tmp-bootstrap-pg.mjs` 等无关未跟踪内容，提交时不要纳入。

## 后续任务卡

以下任务卡按依赖顺序领取。每张卡只处理本卡范围；不要顺手格式化无关文件，也不要把 `.tmp-*`、`docs/user-guide/`、`scripts/tmp-bootstrap-pg.mjs` 纳入提交。

### 卡 0：任务发布热修复与回归（P0）

**目标**：恢复多课程任务的发布能力，并确认发布、分享和学习入口可用。

**前置条件**：由有 Supabase 生产库权限的人执行 `supabase-learning-tasks-gate2b-publish-hotfix.sql`。该脚本仅替换发布 RPC 中的随机 token 调用，不重建任务或名单。

**涉及文件**：

- `supabase-learning-tasks-gate2b.sql`
- `supabase-learning-tasks-gate2b-publish-hotfix.sql`
- `tests/api/learning-task-course-package-publish-hotfix.test.ts`

**实施边界**：不要改任务快照、名单、统计口径；不要新增 hash/SHA-256 逻辑。

**验收清单**：

- 已确认生产 SQL 执行成功。
- 可发布一个包含至少两门课程、至少一名学习对象的草稿任务。
- 学习对象可从 `/student/courses` 与分享链接进入任务；老师预览不写学习数据。
- 定向热修复测试通过，且修改独立提交。

**依赖与输出**：无代码依赖；完成后输出生产执行截图/时间、测试结果和提交号。

### 卡 1：2E 数据中心浏览器验收（P0）

**目标**：确认“任务运营”和“课程资产”双视图在真实登录态下可用，且数据边界正确。

**前置条件**：数据中心提交 `cc9bf405` 已部署；卡 0 的发布回归完成后优先使用真实任务数据。

**涉及文件**：通常不需要改代码；若发现问题，范围限于：

- `components/teaching-data-board.tsx`
- `components/course-analytics-board.tsx`
- `app/api/admin/course-analytics/route.ts`
- `app/api/admin/teaching-data-chat/route.ts`

**验收清单**：

- 教师打开 `/teaching-data`，可切换“任务运营 / 课程资产”。
- 任务运营仅呈现任务、任务包课程和学员维度；不显示逐页 PPT 章节分析。
- 课程资产只显示本人创建课程；`jinzengquan@ruijie.com.cn` 可查看全量课程及被任务引用情况。
- 点击课程数据可进入 `/courses/[id]` 的课程报告；点击任务数据可进入任务管理。
- AI 问答只解释当前权限内的真实汇总数据，不能代替教师执行操作。

**依赖与输出**：依赖卡 0 的真实任务数据；发现缺陷时建立独立修复卡，不把验收和大改混在一次提交中。

### 卡 2：课程管理的数据分析入口（P1）

**状态**：已具备基础入口。课程管理中的本人课程卡已提供“查看数据”，直达 `/courses/[id]`；本卡仅在后续验收发现入口不清晰或权限回归时领取。

**目标**：在课程管理列表中让课程创建者直接进入课程级数据报告，减少从数据中心绕行。

**前置条件**：卡 1 验收通过。

**涉及范围**：课程列表页及其测试；复用现有 `/courses/[id]` 课程报告，不重写课程统计服务。

**实施边界**：

- 课程报告属于课程资产域：跨任务覆盖、学习表现、章节/PPT 分析均在这里。
- 非课程创建者不可从列表获取课程或课程数据入口；全局课程管理员保留全量查看能力。
- 不在任务详情中重新加入章节/PPT 逐页统计。

**验收清单**：

- 有权限的课程卡/列表行有明确“数据分析”入口。
- 无权限课程不可见，也不可通过直链绕过。
- 点击后进入正确课程报告，现有预览/编辑/分享操作不回归。
- TypeScript、定向测试和格式检查通过。

**依赖与输出**：依赖卡 1 确认数据口径；输出独立提交与截图。

### 卡 3：任务运营列表易用性（P1）

**状态**：第一步已完成：任务名称搜索、状态筛选、完成率展示、临近截止与已逾期提示。导出仍待独立实现。

**目标**：让培训运营人员高效定位、筛选和跟进任务。

**前置条件**：卡 0 已恢复发布；卡 1 已确认任务运营口径。

**需求范围**：

- 任务状态、课程、负责人、截止时间的筛选与关键词搜索。
- 清晰标记即将到期、已逾期、补学任务。
- 导出任务汇总与学员明细（仅当前操作者有权限看到的字段）。

**实施边界**：

- 任务是培训运营单元：只呈现任务完成率、学习率、学员和任务包课程进度。
- 导出不得包含标准答案、AI 原始对话或无权课程的数据。
- 不做复杂的自定义报表设计器；先满足常用运营动作。

**验收清单**：

- 多任务环境中可用筛选和搜索定位目标任务。
- 任务列表与详情的状态/完成率一致。
- 导出文件内容与当前筛选范围、权限范围一致。
- 定向测试覆盖权限和一个真实筛选/导出路径。

**依赖与输出**：可与卡 2 并行；输出产品截图、测试结果和独立提交。

### 卡 4：课程级 AI 数据解读（P2，可选）

**目标**：为课程创建者提供课程资产层面的 AI 解读，不与任务运营 AI 混用。

**前置条件**：卡 2 完成，且已有足够跨任务学习数据。

**数据范围**：课程的跨任务覆盖、完成表现、章节学习表现；仅使用当前操作者有权读取的聚合数据。

**实施边界**：AI 只做解释、归纳和建议；不得虚构数据、自动发布任务或代替教师做决定。

**验收清单**：

- 课程报告中可主动生成/查询课程级解读。
- 输出明确引用可见的课程聚合事实，并与任务 AI 文案、入口区分。
- 无数据时给出简洁提示，不编造原因。
- 有 API 权限测试与主路径测试。

### 卡 5：全链路稳定性冒烟与收口（P0，最终）

**目标**：以真实角色和真实数据确认本阶段学习管理闭环可交付。

**前置条件**：卡 0、卡 1 完成；若领取卡 2/3/4，则其验收也完成。

**测试场景**：

1. 课程创建者创建课程，并在课程管理查看自身数据。
2. 培训运营者创建包含多门课程的任务、选择带学员标签的员工、发布并分享。
3. 学员与兼具老师身份的学员均可从 `/student/courses` 进入学习；老师预览不计入学习数据。
4. 学习后任务运营看板显示任务/课程包/学员数据；课程报告显示跨任务和章节数据。
5. AI 简报、补学草稿、驾驶舱数据问答仅使用授权范围的数据。

**验收清单**：记录每个场景的账号角色、URL、结果和异常；P0 问题先修复再交付，P1/P2 问题形成后续卡，不阻塞结项。

**输出**：一份冒烟报告、通过/失败清单、剩余风险和最终提交号。

## 已确立的产品边界

- 课程是内容资产：由创建老师管理，跨任务汇总其使用和学习表现，章节/PPT 分析在课程域。
- 任务是培训运营单元：可组合课程，关注任务完成率、学习率、学员与课程组合进度；不展示逐页 PPT 详情。
- AI 只能解释当前权限范围内的确定性数据；不编造数据，不自动发布补学任务。
- 教师是具备管理权限的学员，不需要在任务名单另分“老师组”。
- 不新增哈希/SHA-256，不做超出实际风险的防御式设计。
