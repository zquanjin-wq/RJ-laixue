# P3：课程资产与腾讯云 COS

> 状态：进行中  
> 日期：2026-09-01

已完成：

- 使用北京地域私有桶完成直传上传、私有读取、音频 Range 读取、短期访问地址和删除验证。
- 应用只保存 COS object key；长期密钥不进入浏览器。
- 课程资产上传接口改为：教师申请短期上传地址，浏览器直传 COS，课程内容保存应用内读取地址。
- 课程资产表记录资产归属、类型、大小和对象路径。
- 课程保存、读取、删除和音频上传接口已切换到 PostgreSQL + COS。
- 文档解析服务读取 COS 短期地址；解析得到的图片回存 COS。

待在 COS 控制台完成一次配置：

- 已配置本地开发来源。正式域名可用时，再增加 `https://laixue.online`；允许方法为 `PUT, GET, HEAD`，允许 Header 为 `Content-Type, Range`。

配置完成后，用浏览器上传一张图片和一段音频即可完成 CORS 与播放的最终验收。
