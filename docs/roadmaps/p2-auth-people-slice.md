# P2：认证与人员管理切片

> 状态：认证切片已实现，随业务切片继续替换旧接口  
> 日期：2026-09-01

已实现：

- Better Auth Next.js API、客户端登录/退出和服务端 session actor。
- 首次管理员初始化命令。
- 管理员创建人员、修改档案、禁用/启用和设置临时密码接口。
- 临时密码首次登录修改流程；改密时撤销其他 session。
- 登录页和共享 `useAuth` 已改用 Better Auth。
- 共享 API guard 已改用 Better Auth actor。

现有课程、学习任务等旧接口仍在后续业务切片中逐个移除 Supabase 数据访问；不能把旧业务接口继续存在误判成认证切片未落地。
