services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: laixue
      POSTGRES_USER: laixue
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set in .env.production}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U laixue -d laixue']
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  app:
    build: .
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
      RENDER_SERVICE_URL: http://render-service:9000
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - app-data:/app/data
    networks:
      - default
      - render
    restart: unless-stopped

  revoice-worker:
    build: .
    env_file:
      - .env.production
    environment:
      COURSE_REVOICE_URL: http://app:3000/api/cron/course-revoice
      COURSE_REVOICE_INTERVAL_MS: '60000'
    depends_on:
      - app
    command: ['node', 'scripts/run-course-revoice-worker.mjs']
    restart: unless-stopped

  render-service:
    build: ./render-service
    profiles: ['video-export']
    expose:
      - '9000'
    cap_add:
      - NET_ADMIN
    environment:
      PORT: '9000'
      RENDER_MAX_CONCURRENCY: '1'
      RENDER_MAX_CONCURRENT_EXTRACTIONS: '1'
      RENDER_MAX_JOBS_PER_USER: '0'
    mem_limit: 4g
    networks:
      - render
    restart: unless-stopped

  video-export-worker:
    build: .
    profiles: ['video-export']
    env_file:
      - .env.production
    environment:
      COURSE_VIDEO_EXPORT_WORKER_URL: http://app:3000/api/cron/course-video-exports
      COURSE_VIDEO_EXPORT_INTERVAL_MS: '5000'
    depends_on:
      - app
      - render-service
    command: ['node', 'scripts/run-course-video-export-worker.mjs']
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    env_file:
      - .env.production
    ports:
      - '80:80'
      - '443:443'
    depends_on:
      - app
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    restart: unless-stopped

  migrate:
    build: .
    profiles: ['tools']
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
    depends_on:
      db:
        condition: service_healthy
    command: ['node', 'scripts/migrate-database.mjs']

  bootstrap-admin:
    build:
      context: .
      target: tools
    profiles: ['tools']
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
    depends_on:
      db:
        condition: service_healthy
    entrypoint: ['node', 'scripts/bootstrap-admin.mjs']

volumes:
  postgres-data:
  app-data:
  caddy-data:
  caddy-config:

networks:
  render:
    internal: true
