services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: laixue
      POSTGRES_USER: laixue
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set in .env.production}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U laixue -d laixue']
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  app:
    build:
      context: .
      args:
        NEXT_PUBLIC_ENABLE_PPTX_IMPORT: ${NEXT_PUBLIC_ENABLE_PPTX_IMPORT:-false}
        NEXT_PUBLIC_ENABLE_DURABLE_GENERATION: ${NEXT_PUBLIC_ENABLE_DURABLE_GENERATION:-false}
        NEXT_PUBLIC_ENABLE_PPTX_AI_CLASSROOM: ${NEXT_PUBLIC_ENABLE_PPTX_AI_CLASSROOM:-false}
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
      RENDER_SERVICE_URL: http://render-service:9000
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - app-data:/app/data
    networks:
      - default
      - render
    restart: unless-stopped

  revoice-worker:
    build: .
    env_file:
      - .env.production
    environment:
      COURSE_REVOICE_URL: http://app:3000/api/cron/course-revoice
      COURSE_REVOICE_INTERVAL_MS: '60000'
    depends_on:
      - app
    command: ['node', 'scripts/run-course-revoice-worker.mjs']
    restart: unless-stopped

  classroom-generation-worker:
    build:
      context: .
      target: tools
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
      CLASSROOM_GENERATION_INTERVAL_MS: '3000'
      CLASSROOM_GENERATION_WORKER_ID: primary-classroom-agent
    depends_on:
      db:
        condition: service_healthy
    command: ['pnpm', 'exec', 'tsx', 'scripts/run-classroom-generation-agent.ts']
    restart: unless-stopped

  course-asset-cleanup-worker:
    build:
      context: .
      target: tools
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
      COURSE_ASSET_CLEANUP_INTERVAL_MS: '21600000'
      COURSE_ASSET_CLEANUP_MAX_AGE_MS: '86400000'
      COURSE_ASSET_CLEANUP_LIMIT: '100'
      COURSE_ASSET_CLEANUP_WORKER_ID: primary-course-asset-cleanup-agent
    depends_on:
      db:
        condition: service_healthy
    command: ['pnpm', 'exec', 'tsx', 'scripts/run-course-asset-cleanup-agent.ts']
    restart: unless-stopped

  render-service:
    build: ./render-service
    profiles: ['video-export']
    expose:
      - '9000'
    cap_add:
      - NET_ADMIN
    environment:
      PORT: '9000'
      RENDER_MAX_CONCURRENCY: '1'
      RENDER_MAX_CONCURRENT_EXTRACTIONS: '1'
      RENDER_MAX_JOBS_PER_USER: '0'
      RENDER_JOB_DEADLINE_MS: '5400000'
      # Hyperframes defaults every FFmpeg preprocessing command to 5 minutes.
      # Long narrated courses can legitimately exceed that while mixing many
      # audio clips (the 46m55s / 179-track course took ~310s), so keep the
      # audio/encode subprocess budget below, but proportional to, the 90m job
      # deadline. This preserves fail-closed audio correctness instead of
      # allowing a silent video fallback.
      FFMPEG_PROCESS_TIMEOUT_MS: '1800000'
      FFMPEG_ENCODE_TIMEOUT_MS: '1800000'
    # 4 vCPU / 16 GB production profile: leave one CPU for PostgreSQL, the web
    # app and workers while allowing the producer to exit low-memory mode.
    cpus: 3.0
    mem_limit: 10g
    networks:
      - render
    restart: unless-stopped

  video-export-worker:
    build:
      context: .
      target: tools
    profiles: ['video-export']
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
      RENDER_SERVICE_URL: http://render-service:9000
      COURSE_VIDEO_EXPORT_INTERVAL_MS: '5000'
      COURSE_VIDEO_EXPORT_LEASE_MS: '300000'
      COURSE_VIDEO_EXPORT_WORKER_ID: primary-video-agent
    depends_on:
      db:
        condition: service_healthy
      render-service:
        condition: service_started
    networks:
      - default
      - render
    command: ['pnpm', 'exec', 'tsx', 'scripts/run-course-video-export-agent.ts']
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    env_file:
      - .env.production
    ports:
      - '80:80'
      - '443:443'
    depends_on:
      - app
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    restart: unless-stopped

  migrate:
    build: .
    profiles: ['tools']
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
    depends_on:
      db:
        condition: service_healthy
    command: ['node', 'scripts/migrate-database.mjs']

  bootstrap-admin:
    build:
      context: .
      target: tools
    profiles: ['tools']
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgres://laixue:${POSTGRES_PASSWORD}@db:5432/laixue
    depends_on:
      db:
        condition: service_healthy
    entrypoint: ['node', 'scripts/bootstrap-admin.mjs']

volumes:
  postgres-data:
  app-data:
  caddy-data:
  caddy-config:

networks:
  render:
    internal: true
